RSpec.describe Activeadmin::Nested::Namespaces do
  it "has a version number" do
    expect(Activeadmin::Nested::Namespaces::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
